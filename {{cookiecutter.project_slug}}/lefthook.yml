skip_output:
  - meta
  - success
  - summary
pre-commit:
  commands:
    black:
      tags: formatter
      run: black --check -- {staged_files}
    isort:
      tags: formatter
      run: isort --check -- {staged_files}
